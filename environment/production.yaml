# Shared Kanopy values for ALL retail-stock-take releases (production).
# Each Drone deploy step loads this base PLUS environment/production-<service>.yaml.
security:
  dataClassification: "Internal"
  dataDesignation: ["MI"]

ownership:
  driEmail: alberto.camarena@mongodb.com

serviceAccount:
  enabled: true
  irsa:
    accountId: "275662791714"
    roleName: "kanopy-prod-cicd-irsa"

env:
  AWS_REGION: us-east-1
